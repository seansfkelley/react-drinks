fixed-header-container(height)
  height 100%

  .fixed-header-bar
    height height

  .fixed-header-content-pane
    height "calc(100% - %s)" % height
    overflow-y scroll
    -webkit-overflow-scrolling touch
